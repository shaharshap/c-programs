#include<stdio.h>
#include<string.h>
int main()
{
	char s[30];
	int i;
	gets(s);
	for(i=0;s[i]!=\'0';i++)
	{
		if((i%2)==0)
		s[i]=tolower(s[i]);
		else
		s[i]=toupper(s[i]);
	}
	printf("\n%s",s);
	return 0;
}
Input: Enter string:aditya
Output:aDiTyA
